# Query
extend type Query {
  items: [Item!]!
}

# Types
type Item {
  id: ID!
  name: String!
  price: Float!
}

type Item {
  id: ID!
  name: String!
  price: Float!
}

type ItemSelection {
  id: ID!
  name: String!
  price: Float!
  amount: Int!
}
